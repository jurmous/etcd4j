sudo: required
install: true
language: java

jdk: openjdk8

services:
  - docker

script:
  - java -version
  - ./gradlew ci

after_failure:
  - "cat ./build/etcd4j-test.log"
  - "cat ./build/reports/dockerTest/index.html"
